{% extends "::layout/layout.html.twig" %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'attr': {'class': 'form-horizinal'}}) }}
        <div>
            {{ include('mesformulaire/relever.html.twig') }}<br>
            
        </div>
            <P style="text-align: center">
                <button class="btn btn-primary boutonCateEn">Valider <span class="fa fa-check"></span></button>
                <span class="fa fa-reply"> <a style="text-decoration: none; color: blue;" href="{{path('utilisateur_collecteur', { 'id' : id }) }}">Retour</a></span>
            </P>
        {{ form_end(form) }}      
    </div>
{% endblock %}