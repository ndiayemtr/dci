<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title"></h3>
    </div>

    <div class="box-body">
        <div class="row">
            <h4 style="color: #0000F0; text-align: center">Modification de l'Indicateur</h4><br>
            <div class="col-md-5">   
                
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.reference, "Reference") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.reference) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.reference, {'attr': {'class': 'form-control', 'placeholder':'reference'}}) }}
                    </div>          

                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.libelle, "Libelle") }}</div>
                    <div class="col-md-7">

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.libelle) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.libelle, {'attr': {'class': 'form-control', 'placeholder':'libelle'}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.typeData, "Type de Donnée") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.typeData) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.typeData, {'attr': {'class': 'form-control', 'placeholder':''}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.unite, "Unite") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.unite) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.unite) }}
                    </div>
                </div><br><br>
            
            </div>
            <div class="col-md-5">
                <div class="form-group">
                    <div class="col-md-3"> {{ form_label(form.mode, "Mode") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.mode) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.mode, {'attr': {'class': 'form-control', 'placeholder': 'mode'}}) }}
                    </div>
                </div><br><br>
                    <div class="form-group">
                    <div class="col-md-3">{{ form_label(form.periodicite, "Periode") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.periodicite) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.periodicite, {'attr': {'class': 'form-control periodicite', 'placeholder':'clic pour choisir'}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-3">{{ form_label(form.niveau, "Niveau") }}</div>
                    <div class="col-md-7">            
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.niveau) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.niveau, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <br><br><br>
                <div class="form-group">
                    <div class="col-md-3">{{ form_label(form.dateIndi, "date Indi") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.dateIndi) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.dateIndi, {'attr': {'class': 'form-control periodicite', 'placeholder':'clic pour choisir'}}) }}
                    </div>
                </div><br><br>
                
                               
            </div>
        </div>
    </div>
                    </div>

</div><!-- box box-primary-->
