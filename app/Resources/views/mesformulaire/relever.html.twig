<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title"></h3>
    </div>

    <div class="box-body">
        <div class="row">
            <div class="col-md-offset-4 col-md-5">
                <h4 style="color: #0000F0">Ajouter une nouvelle Attribut d'entite</h4><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.dateDuRelever, "date du relevé") }}</div>
                    <div class="col-md-7" id="dateDuRelever">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.dateDuRelever) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.dateDuRelever, {'attr': {'class': 'form-control dateDuRelever'}}) }}
                    </div>          
            </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.quantite, "Quantité") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.quantite) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.quantite, {'attr': {'class': 'form-control', 'placeholder':'Attribut Entite'}}) }}
                    </div>          
            </div>

        </div>
    </div>

</div><!-- box box-primary-->