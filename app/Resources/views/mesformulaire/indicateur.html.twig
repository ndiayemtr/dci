<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title"></h3>
    </div>

    <div class="box-body">
        <div class="row">
            <h4 style="color: #0000F0; text-align: center">Ajouter un nouveau Indicateur</h4><br>
            <div class="col-md-5">   
                
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.libelle, "Libelle") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.libelle) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.libelle, {'attr': {'class': 'form-control', 'placeholder':'libelle'}}) }}
                    </div>          

                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.origine, "Origine") }}</div>
                    <div class="col-md-7">

                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.origine) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.origine, {'attr': {'class': 'form-control', 'placeholder':'origine'}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.producteur, "Producteur") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.producteur) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.producteur, {'attr': {'class': 'form-control', 'placeholder':'Producteur'}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.departement, "Département") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.departement) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.departement) }}
                    </div>
                </div><br><br>
            
            </div>
            <div class="col-md-5">
                <div class="form-group">
                    <div class="col-md-5"> {{ form_label(form.unite, "Unité") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.unite) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.unite, {'attr': {'class': 'form-control', 'placeholder': 'exemple Kg'}}) }}
                    </div>
                </div><br><br>
                    <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.periodicite, "Periode") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.periodicite) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.periodicite, {'attr': {'class': 'form-control periodicite', 'placeholder':'clic pour choisir'}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.typeCondionnement, "type de Condionnement") }}</div>
                    <div class="col-md-7">            
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.typeCondionnement) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.typeCondionnement, {'attr': {'class': 'form-control', 'placeholder':'type Condionnement'}}) }}
                    </div>
                </div><br><br>
                
                               
            </div>
        </div>
    </div>

</div><!-- box box-primary-->