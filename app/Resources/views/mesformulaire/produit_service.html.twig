<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title"></h3>
    </div>
    

    <div class="box-body">

        <div class="form-group">
            {{ form_label(form.code, "Code Produit/Service") }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.code) }}
            {# Génération de l'input. #}
            {{ form_widget(form.code, {'attr': {'class': 'form-control', 'placeholder':'Code Produit/Service'}}) }}
        </div>
                <div class="form-group">
            {{ form_label(form.libelle, "Libelle Produit/Service") }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.libelle) }}
            {# Génération de l'input. #}
            {{ form_widget(form.libelle, {'attr': {'class': 'form-control', 'rows': '4','cols': '4'}}) }}
        </div>
        <div class="form-group">
            {{ form_label(form.categorie, "Produit ou Service") }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.categorie) }}
            {# Génération de l'input. #}
            {{ form_widget(form.categorie) }}
        </div>

    </div>


</div><!-- box box-primary-->