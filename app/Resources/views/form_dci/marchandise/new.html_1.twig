<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title"></h3>
    </div>

    <div class="box-body">
        <div class="row">
            <div class="col-md-5">   
                {#<h4 style="color: #0000F0">Ajouter une nouvelle entité</h4>#}
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.reference, "Réference") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.reference) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.reference, {'attr': {'class': 'form-control', 'placeholder':'UY/654'}}) }}
                    </div>          

                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.stade, "Stade") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.stade) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.stade, {'attr': {'class': 'form-control', 'placeholder':'stade'}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.origine, "Origine") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.origine) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.origine, {'attr': {'class': 'form-control', 'placeholder':'origine'}}) }}
                    </div>
                </div><br><br>
            
            </div>
            <div class="col-md-5">
                <div class="form-group">
                    <div class="col-md-5"> {{ form_label(form.dateMarchandise, "Date") }}</div>
                    <div class="col-md-7">
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.dateMarchandise) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.dateMarchandise, {'attr': {'class': 'periodicite', 'placeholder':'clic pour choisir une date'}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.libelle, "Nom de la marchandise") }}</div>
                    <div class="col-md-7">            
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.libelle) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.libelle, {'attr': {'class': 'form-control', 'placeholder':'Nom de la marchandise'}}) }}
                    </div>
                </div><br><br>
                <div class="form-group">
                    <div class="col-md-5">{{ form_label(form.typeMarchandise, "Type de la marchandise") }}</div>
                    <div class="col-md-7">            
                        {# Affichage des erreurs pour ce champ précis. #}
                        {{ form_errors(form.typeMarchandise) }}
                        {# Génération de l'input. #}
                        {{ form_widget(form.typeMarchandise, {'attr': {'class': 'form-control', 'placeholder':'Type de marchandise'}}) }}
                    </div>
                </div><br><br>
                               
            </div>
        </div>
    </div>

</div><!-- box box-primary-->